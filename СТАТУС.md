# Гравировка neo36 — ACMER S2

## Что сделано
- ✅ Драйвер CH340 — работает (модуль ch341 в Linux)
- ✅ Udev правило — `/etc/udev/rules.d/99-acmer-laser.rules` (порт всегда rw)
- ✅ Пользователь n36 добавлен в группу dialout
- ✅ Python-скрипт гравировки — `/home/n36/laser_neo36/engrave_neo36.py`
- ✅ G-код сгенерирован — `/home/n36/laser_neo36/neo36.gcode`
- ✅ Превью текста — `/home/n36/laser_neo36/neo36_preview.png`
- ✅ GRBL 1.1h обнаружен на /dev/ttyUSB0
- ✅ Хоминг ($H) проходит успешно
- ❌ Стол (голова) НЕ ЕДЕТ во время гравировки

## Главная проблема
**Лазер мигает, голова не движется.**

Возможные причины (по приоритету):
1. **Шаговые драйверы не вставлены / вставлены неправильно** на плате
   - Открой корпус, найди платки с радиаторами (A4988/DRV8825), плотно вставь
2. **Обрыв/перепутан кабель мотора** (X или Y)
   - Проверь разъёмы моторов на плате и на самих моторах
3. **Неисправный блок питания** (мало тока для моторов)
   - Нужен минимум 5А при 12В
4. **Прошивка GRBL** — $21=1 (жёсткие концевики) + Pn:X всегда активен
   - Если концевик X не работает, GRBL может блокировать движение

## GRBL настройки
```
$21=1  жёсткие лимиты включены
$22=1  хоминг включён
$32=1  лазерный режим ВКЛ
$30=1000  макс. мощность
$100=80  шагов/мм X
$101=80  шагов/мм Y
$130=300  рабочая зона X мм
$131=300  рабочая зона Y мм
```

## Как продолжить после перезагрузки

```bash
# 1. Проверь порт
ls -la /dev/ttyUSB0

# 2. Запусти гравировку
python3 /home/n36/laser_neo36/engrave_neo36.py
```

Скрипт сам делает: reset → хоминг → гравировка neo36

## Параметры гравировки
- Размер текста: ~95 × 18 мм
- Мощность лазера: 65% (650/1000)
- Скорость: 2500 мм/мин
- Шаг строк: 0.12 мм

## Диагностика если стол всё ещё не едет

```bash
python3 << 'EOF'
import serial, time
s = serial.Serial('/dev/ttyUSB0', 115200, timeout=3)
time.sleep(2); s.flushInput()
s.write(b'\x18'); time.sleep(2); s.flushInput()
s.write(b'?'); time.sleep(0.5)
print(s.read(s.in_waiting).decode())
# Послать тестовое движение 10мм
s.write(b'$X\n'); time.sleep(1)
s.write(b'G91\n'); time.sleep(0.5)
s.write(b'G0 F500 X10\n'); time.sleep(3)
s.write(b'?'); time.sleep(0.5)
print(s.read(s.in_waiting).decode())
s.close()
EOF
```

Если после G0 X10 позиция не изменилась — проблема в механике/электрике мотора.
